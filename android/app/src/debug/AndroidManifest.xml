<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dev builds may talk to a local backend over HTTP (emulator loopback).
         Release builds never allow cleartext. -->
    <application android:usesCleartextTraffic="true" tools:ignore="UnusedAttribute" xmlns:tools="http://schemas.android.com/tools" />
</manifest>